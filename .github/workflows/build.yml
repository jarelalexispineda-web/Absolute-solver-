name: Build APK
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 18

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 11

      - name: Preparar proyecto
        run: |
          mkdir www
          cp "Absolute-solber.html" www/index.html
          npm init -y
          npm install @capacitor/core @capacitor/cli @capacitor/android

      - name: Inicializar Capacitor
        run: |
          npx cap init "AbsoluteSolver" "com.absolute.solver" --web-dir www
          npx cap add android
          npx cap sync android

      - name: Compilar APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon -Dorg.gradle.jvmargs="-Xmx1g"

      - uses: actions/upload-artifact@v4
        with:
          name: absolute-solver-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
